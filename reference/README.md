# Stable reference

This directory stores metadata only. Do not commit extracted upstream bundles,
executables, authentication data, session history, or OpenAI assets.

`stable-26.721.3996.0/manifest.json` fingerprints the locally installed build
used as the executable specification.
